"""Rate-limiting placeholder middleware utilities.

SRS coverage placeholder only. No behavior is implemented yet.
"""


def enforce_rate_limit(*, scope: str, subject: str) -> None:
    """TODO: enforce brute-force protection for auth/submission routes."""
    _ = (scope, subject)
    pass
